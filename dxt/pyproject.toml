[project]
name = "mac-letterhead-dxt"
version = "0.14.3"
description = "Desktop Extension entry point for Mac-letterhead MCP server"
requires-python = ">=3.10"
dependencies = [
    "mac-letterhead[mcp]>=0.14.3",
]
